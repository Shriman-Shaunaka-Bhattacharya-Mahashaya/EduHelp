version: '3.8'
services:
  exam-system:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: exam-system-app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
